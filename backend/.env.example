# ── Django ──
DJANGO_SECRET_KEY=change-me-in-production
ALLOWED_HOSTS=localhost,127.0.0.1

# ── Database (production) ──
DB_NAME=shaffoftir
DB_USER=shaffoftir
DB_PASSWORD=shaffoftir_secret
DB_HOST=localhost
DB_PORT=5432

# ── CORS ──
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
